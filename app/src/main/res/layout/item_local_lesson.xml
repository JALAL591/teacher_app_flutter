<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
>
<LinearLayout
    android:id="@+id/draftItemRoot"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingStart="16dp"
    android:paddingEnd="16dp"
    android:background="?attr/selectableItemBackground">

    <!-- Avatar مربع cornerRadius 8dp أصفر #FFC107 -->
    <FrameLayout
        android:id="@+id/lessonImage"
        android:layout_width="54dp"
        android:layout_height="54dp"
        android:background="@drawable/lesson_avatar_draft">

        <TextView
            android:id="@+id/lessonEmojiText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="&#x1F4DD;"
            android:textSize="20sp"
            android:textColor="?attr/colorOnSurface"
            android:contentDescription="@string/desc_lesson_image" />

    </FrameLayout>

    <!-- معلومات الدرس -->
    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp">

        <TextView
            android:id="@+id/lessonTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/sample_lesson_title"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="?attr/colorOnSurface"
            android:maxLines="1"
            android:ellipsize="end" />

        <TextView
            android:id="@+id/unitText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/label_unit_name"
            android:textSize="14sp"
            android:textColor="?attr/colorOnSurfaceVariant"
            android:layout_marginTop="2dp"
            android:maxLines="1"
            android:ellipsize="end" />

    </LinearLayout>

    <!-- زر بث يسار -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/publishButton"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:text="@string/btn_publish_short"
        android:textSize="13sp"
        android:textStyle="bold"
        android:textColor="?attr/colorSurface"
        android:backgroundTint="?attr/colorPrimary"
        app:cornerRadius="100dp"
        android:paddingStart="14dp"
        android:paddingEnd="14dp" />

</LinearLayout>

</layout>