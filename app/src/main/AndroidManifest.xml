<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edu.teacher">
    
    <!-- Network Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    
    <!-- Hotspot/Tethering Permissions -->
    <uses-permission android:name="android.permission.WRITE_SETTINGS" android:maxSdkVersion="28" />
    
    <!-- Bluetooth Permissions (Android 12+) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" android:usesPermissionFlags="neverForLocation" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    
    <!-- Location Permissions (Required for WiFi scanning) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Nearby WiFi Devices (Android 13+) -->
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" android:usesPermissionFlags="neverForLocation" />
    
    <!-- Notifications Permission (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Battery Optimization -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <!-- Android 16 Specific Permissions -->
    <uses-permission android:name="android.permission.MANAGE_WIFI_NETWORK_SELECTION" />
    
    <!-- Features -->
    <uses-feature android:name="android.hardware.wifi.direct" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.wifi" android:required="false" />

<!-- صلاحية الظهور فوق التطبيقات الأخرى (ضرورية لـ Samsung Android 14+) -->
<uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <application
        android:name=".TeacherApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".RoleSelectionActivity"
            android:exported="true"
            android:configChanges="uiMode|screenSize|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".LoginActivity"
            android:exported="false"
            android:configChanges="uiMode|screenSize|orientation" />

        <activity android:name=".DashboardActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".AddClassActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".AddLessonActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".EditLessonActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".ClassManagerActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".SyncActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".StatsActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".StudentsActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".SettingsActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name=".LessonDetailsActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />

        <!-- Teacher Stats Activities -->
        <activity
            android:name=".ui.stats.LessonSubmissionsActivity"
            android:exported="false"
            android:screenOrientation="portrait" />

        <!-- Student App Activities -->
        <activity android:name="com.edu.student.ui.login.ActivationActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name="com.edu.student.ui.dashboard.DashboardActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name="com.edu.student.ui.subject.SubjectActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name="com.edu.student.ui.lesson.LessonActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name="com.edu.student.ui.stats.StatsActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name="com.edu.student.ui.sync.SyncActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />
        <activity android:name="com.edu.student.ui.settings.SettingsActivity" android:exported="false" android:configChanges="uiMode|screenSize|orientation" />

<provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        
        <!-- Teacher Server Service -->
        <service
            android:name=".TeacherServerService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />
        
    </application>
</manifest>
